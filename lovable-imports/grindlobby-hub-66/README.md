# GrindLobby Hub (66)

Lovable project: `3d4b96d6-4c11-4cb7-aae2-22170b98e6da`

Snapshot inventory: 20 authored/config files were exported in Lovable. The route `/` remains the starter placeholder, so this version is not suitable as the active UI. Useful authored material:

- `src/data/mock.ts`
- `src/data/types.ts`
- `src/store/app-store.tsx`
- `src/components/gl/primitives.tsx`
- `src/styles.css`
- TanStack router/start files and configs.

The design system uses deep graphite surfaces and violet tokens, but the custom `GLogo` SVG is obsolete. When any primitive is reused, replace it with `/brand/grindlobby-official.png`.

This snapshot is archived as a component/data reference only. Generic shadcn boilerplate, node_modules, lockfiles and generated route tree are intentionally omitted.
