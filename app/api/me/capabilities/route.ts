import {getCurrentUser} from "@/lib/auth";import {getAgeAssurance,getAgeCapabilities} from "@/lib/age-assurance";import {getScreenSharePolicy} from "@/lib/livekit-screen-policy";import {noStoreJson} from "@/lib/security/request";
export async function GET(){const user=await getCurrentUser();if(!user)return noStoreJson({error:"Não autorizado."},{status:401});const pro=user.account_tier==="pro"||user.app_role==="admin",screenShare=getScreenSharePolicy(pro),assurance=await getAgeAssurance(user.id),age=getAgeCapabilities(assurance);return noStoreJson({screenShare:{...screenShare,allowed:age.canScreenShare,reason:age.canScreenShare?null:age.reason},store:{premiumAccess:pro,allTestItems:user.app_role==="admin",purchasesAllowed:age.canPurchase},age:{assurance,capabilities:age}})}
