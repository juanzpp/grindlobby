"use client";
import Link from "next/link";import {FormEvent,useState} from "react";import AuthShell from "@/components/auth/AuthShell";import GrindLoading from "@/components/feedback/GrindLoading";
export default function ForgotPassword(){const [email,setEmail]=useState(""),[loading,setLoading]=useState(false),[sent,setSent]=useState(false);async function submit(event:FormEvent){event.preventDefault();setLoading(true);try{await fetch("/api/auth/forgot-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})})}finally{setSent(true);setLoading(false)}}return <AuthShell eyebrow="ACCOUNT RECOVERY" title="Recupere sua senha" description="Enviaremos um link seguro para redefinir o acesso."><form className="auth-form" onSubmit={submit}>{sent?<div className="auth-success">Se existir uma conta para este e-mail, você receberá as instruções em breve.</div>:null}<label className="auth-label">E-mail<input className="auth-input" type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" required/></label><button className="auth-primary" disabled={loading}>{loading?<GrindLoading label="Enviando…"/>:"Enviar link"}</button><Link className="auth-back" href="/login">Voltar para login</Link></form></AuthShell>}
