"use client";
import {FormEvent,useState} from "react"; import {useRouter} from "next/navigation";
export default function Login(){
 const [identifier,setIdentifier]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false),router=useRouter();
 async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({identifier,password})});const d=await r.json();setLoading(false);if(!r.ok)return setError(d.error);router.push("/");router.refresh();}
 return <main className="grid min-h-screen place-items-center p-5"><div className="w-full max-w-md"><div className="mb-8 text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-violet-600 text-xl font-black">G</div><h1 className="mt-4 text-2xl font-black">GRIND<span className="text-violet-400">LOBBY</span></h1><p className="mt-2 text-sm text-zinc-500">Entre na sua gaming network.</p></div><form onSubmit={submit} className="glass rounded-3xl p-6"><label className="mb-4 block text-sm">Username ou email<input className="field mt-2" value={identifier} onChange={e=>setIdentifier(e.target.value)} autoComplete="username"/></label><label className="block text-sm">Senha<input className="field mt-2" type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/></label>{error&&<div className="mt-4 rounded-xl border border-red-500/20 bg-red-500/10 p-3 text-sm text-red-300">{error}</div>}<button disabled={loading} className="mt-5 w-full rounded-xl bg-violet-600 py-3 font-bold disabled:opacity-50">{loading?"Entrando...":"Entrar"}</button><p className="mt-5 text-center text-sm text-zinc-500">Ainda não tem conta? <a href="/register" className="text-violet-400">Criar conta</a></p></form></div></main>
}