"use client";
import {FormEvent,useState} from "react"; import {useRouter} from "next/navigation";
export default function Register(){
 const [form,setForm]=useState({username:"",email:"",password:"",displayName:""}),[error,setError]=useState(""),[loading,setLoading]=useState(false),router=useRouter();
 const change=(k:keyof typeof form,v:string)=>setForm({...form,[k]:v});
 async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");const r=await fetch("/api/auth/register",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(form)});const d=await r.json();setLoading(false);if(!r.ok)return setError(d.error);router.push("/");router.refresh();}
 return <main className="grid min-h-screen place-items-center p-5"><div className="w-full max-w-md"><div className="mb-8 text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-violet-600 text-xl font-black">G</div><h1 className="mt-4 text-2xl font-black">Criar conta</h1><p className="mt-2 text-sm text-zinc-500">Entre para começar seu grind.</p></div><form onSubmit={submit} className="glass rounded-3xl p-6"><label className="mb-4 block text-sm">Nome<input className="field mt-2" value={form.displayName} onChange={e=>change("displayName",e.target.value)}/></label><label className="mb-4 block text-sm">Username<input className="field mt-2" value={form.username} onChange={e=>change("username",e.target.value)} autoComplete="username"/></label><label className="mb-4 block text-sm">Email<input className="field mt-2" type="email" value={form.email} onChange={e=>change("email",e.target.value)} autoComplete="email"/></label><label className="block text-sm">Senha<input className="field mt-2" type="password" value={form.password} onChange={e=>change("password",e.target.value)} autoComplete="new-password"/></label>{error&&<div className="mt-4 rounded-xl border border-red-500/20 bg-red-500/10 p-3 text-sm text-red-300">{error}</div>}<button disabled={loading} className="mt-5 w-full rounded-xl bg-violet-600 py-3 font-bold disabled:opacity-50">{loading?"Criando...":"Criar conta"}</button><p className="mt-5 text-center text-sm text-zinc-500">Já tem conta? <a href="/login" className="text-violet-400">Entrar</a></p></form></div></main>
}