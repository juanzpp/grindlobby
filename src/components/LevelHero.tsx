import { ChevronDown, Info, Sparkle } from "lucide-react";
import { ProfileAvatar } from "@/components/ProfileAvatar";
import { usePlayer, findItem } from "@/lib/player-store";
import { LEVEL_TIERS, MAX_LEVEL, getTier, tierAuraShadow, tierGradient } from "@/lib/levels";

export function LevelHero({onOpenProfile}:{onOpenProfile:()=>void}){const{player}=usePlayer();const tier=getTier(player.level);const need=1000;const pct=Math.min(100,Math.round((player.xp/need)*100));const title=findItem(player.equipped.title)?.label;const nextTier=LEVEL_TIERS.find(t=>t.from>player.level);
 return <section className="panel hero-surface relative overflow-hidden px-5 py-6"><div className="grid items-center gap-6 lg:grid-cols-[auto_1fr_auto]"><div className="relative mx-auto"><span className="absolute inset-0 -z-10 rounded-full blur-2xl" style={{background:tier.color,opacity:.35}}/><img src="/grindlobby-logo.png" alt="Emblema GrindLobby" className="h-40 w-40 object-contain" style={{filter:`drop-shadow(0 0 34px ${tier.glow})`}}/></div><div><p className="label-caps">Seu progresso</p><div className="mt-1 flex flex-wrap items-center gap-3"><h1 className="font-display text-4xl font-bold">Level {player.level}</h1><span className="rounded-md px-2.5 py-1 font-display text-[11px] font-bold tracking-wide text-background" style={{backgroundImage:tierGradient(tier)}}>{tier.name.toUpperCase()}</span><span className="btn-ghost flex items-center gap-2 rounded-lg px-3 py-1.5 text-xs font-medium"><Sparkle className="h-3.5 w-3.5 text-primary-glow"/>{player.game}<ChevronDown className="h-3.5 w-3.5 text-muted-foreground"/></span></div><div className="mt-4"><div className="flex items-center justify-between text-xs text-muted-foreground"><span>XP do level</span><span><b className="text-foreground">{player.xp}</b> / {player.level>=MAX_LEVEL?"MAX":need} XP</span></div><div className="relative mt-2 h-3.5 overflow-hidden rounded-full bg-secondary"><div className="h-full rounded-full" style={{width:`${pct}%`,backgroundImage:tierGradient(tier),boxShadow:tierAuraShadow(tier)}}/></div><p className="mt-2 text-xs text-muted-foreground">{player.level>=MAX_LEVEL?"Level máximo alcançado":`Faltam ${need-player.xp} XP para o level ${player.level+1}`}{nextTier&&player.level<MAX_LEVEL&&<> • próximo elo: <span style={{color:nextTier.glow}}>{nextTier.name}</span> no level {nextTier.from}</>}</p><p className="mt-2 text-xs text-emerald-400">XP e elo são concedidos somente por partidas finalizadas e confirmadas pelo servidor.</p></div><div className="mt-4"><button onClick={onOpenProfile} className="btn-ghost rounded-lg px-3 py-2 text-xs font-medium">Configurar perfil</button></div></div><div className="flex flex-col items-center gap-3 lg:border-l lg:border-border lg:pl-6"><ProfileAvatar name={player.nickname} size={78} borderId={player.equipped.border} avatarUrl={player.avatarUrl}/><div className="text-center"><p className="flex items-center justify-center gap-2 font-display text-lg font-bold">{player.nickname}{title&&<span className="rounded border border-primary/50 bg-primary/15 px-1.5 text-[10px] font-bold text-primary-glow">{title}</span>}</p><p className="text-xs text-muted-foreground">@{player.handle}</p><p className="mt-2 flex items-center justify-center gap-1.5 text-xs text-muted-foreground">{player.region}<Info className="h-3.5 w-3.5"/></p></div></div></div></section>}
